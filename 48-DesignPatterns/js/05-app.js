// Organizar código
// Module, Probablemente el más popular de todos principalmente en JS sobre todo en frameworks
// Su Sintaxis era un poco diferente antes, ahora ya tenemos modules en JavaScript por lo tanto ya se siente como una solución más natural
// ya que antes era como el siguiente ejemplos


const mostrarCliente = nombre => {
    console.log(nombre);
}

export default mostrarCliente


var module1 = (function() {

})();