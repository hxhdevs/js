// .toUpperCase() te va a permitir cambiar un texto a todo mayusculas
const producto = 'Monitor 24 pulgadas ';
console.log(producto.toUpperCase() );

// .toLowerCase() transforma todo a minusculas
console.log(producto.toLowerCase() );

// .toString()
const cantidad = 200;
// si el numero aparece en el azul en consola quiere decir que es de tipo numerico
console.log(cantidad);
//si el numero aparece en color negro en consola es String aunque sea un numero
console.log(cantidad.toString());